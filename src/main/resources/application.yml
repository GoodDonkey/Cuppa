spring:
  profiles:
    default: dev

  messages:
    basename: message, errors

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    com:
      cuppa:
        cuppa: debug